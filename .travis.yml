language: ruby

sudo: required

services:
  - docker

before_install:
  - docker-compose build

install:
  - gem install bundler --version '1.7.4'
script: "rake"

after_succes:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker compose push alexblaj/cloudapps