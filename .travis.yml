sudo: required

services:
  - docker

before_install:
  - docker-compose build -t alexblaj/cloudapps .

script:
  - docker run alexblaj/cloudapps ./star.sh

after_succes:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push alexblaj/cloudapps

